type bool := {
};

type nat := {
	//arithmetic operators
	+: (nat, nat) -> nat;
	-: (nat, nat) -> nat;
	*: (nat, nat) -> nat;
	
	//logical operators
	<=: (nat, nat) -> bool;
};

type string := {
	+: (string, string) -> string;
};

type unit := {
};

let print: any -> unit := extern "print";
