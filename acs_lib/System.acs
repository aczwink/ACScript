type nat := {
	//arithmetic operators
	+: (nat, nat) -> nat;
	-: (nat, nat) -> nat;
	*: (nat, nat) -> nat;
};

type string := {
	+: (string, string) -> string;
};

type unit := {
};

let print: any -> unit := extern "print";
