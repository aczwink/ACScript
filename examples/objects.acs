vec := < x: 0, y: 0 >;

vec.add := func (dx, dy) => {
	this.x += dx;
	this.y += dy;
}

vec.scale := func scale => {
	this.x *= scale;
	this.y *= scale;
}

Print(vec);
vec.add(1, 2);
Print(vec);
vec.scale(2);
Print(vec);